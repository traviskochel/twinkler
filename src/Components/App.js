var React = require('react');
var StarsWrapper = require('./StarsWrapper');

var App = React.createClass({
  render: function() {
    return (
      <StarsWrapper />
    );
  }
})

module.exports = App;
